<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <service android:name="com.lzh.nonview.router.host.RouterHostService"
            android:process=":routerHostService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.lzh.router.action.HOST"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </service>
    </application>

</manifest>
